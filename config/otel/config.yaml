receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
processors:
  memory_limiter:
    check_interval: 5s
    limit_percentage: 90
    spike_limit_percentage: 20
  batch:
    send_batch_max_size: 512
    send_batch_size: 256
    timeout: 8s
exporters:
  file/traces:
    path: /etc/otelcol/.data/output_traces.log
  otlp/tempo:
    endpoint: localhost:4417
    tls:
      insecure: true
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [otlp/tempo, file/traces]
